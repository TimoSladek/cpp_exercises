#include<bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    if (n == m) {
        cout << "Hura, rovnaju sa." << endl;
    } else {
        cout << "Nerovnaju sa." << endl;
    }
    return 0;
}